rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{user_id} {
      allow create: if request.auth != null;
      allow update, read: if request.auth.uid == user_id;
    }
  }
}
